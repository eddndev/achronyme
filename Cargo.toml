[workspace]
members = [
    "diagnostics",
    "memory",
    "akron",
    "akron-llvm",
    "akron-aot-runtime",
    "akronc",
    "constraints",
    "achronyme-parser",
    "cli",
    "ir",
    "std",
    "ach-macros",
    "proving",
    "circom",
    "resolve",
    "artik",
    "lysis",
    "lysis-types",
    "ir-core",
    "ir-forge",
    "zkc",
    "prove-engine",
]
exclude = ["wasm"]
resolver = "2"

[workspace.package]
version = "0.1.1"
edition = "2021"
license = "Apache-2.0"
repository = "https://github.com/achronyme/achronyme"
homepage = "https://achrony.me"
authors = ["Achronyme Contributors"]
keywords = ["zero-knowledge", "zk", "circuits", "compiler", "cryptography"]
categories = ["compilers", "cryptography"]

[profile.release]
lto = true
opt-level = 3
codegen-units = 1
panic = "abort"

# Montgomery field arithmetic and the R1CS optimizer's row reduction
# dominate debug-profile test time; unoptimized they make the workspace
# suite take over an hour. Optimizing only these math-heavy leaf crates
# keeps the crates under test debuggable, and debug_assertions stay on
# (opt-level overrides do not disable them).
[profile.dev.package.memory]
opt-level = 3

[profile.dev.package.constraints]
opt-level = 3

# Release-equivalent profile that retains line-table debuginfo so
# external sampling profilers (samply, perf, pprof-rs) can resolve
# Rust frames. Used by the `profile_compile_cpu` example. Compile
# time ~ release; binary size grows by the line tables only.
[profile.profile-cpu]
inherits = "release"
debug = "line-tables-only"
strip = "none"
